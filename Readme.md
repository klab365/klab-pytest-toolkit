# klab-pytest-toolkit

A Python App from template

## Getting Started

Every command is available in the `Justfile` and can be run with `just <command>`.

1. Install Dependencies with poetry

```bash
just install
```
